{
  "compilerOptions": {
    /* Base Options: */
    "esModuleInterop": true,
    "skipLibCheck": true,
    "target": "es2022",
    "allowJs": true,
    "resolveJsonModule": true,
    "moduleDetection": "force",
    "isolatedModules": true,
    "typeRoots": ["node_modules/@types", "src/types"],

    /* Strictness */
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,

    /* If transpiling with TypeScript: */
    "module": "NodeNext",
    "outDir": "dist",
    "sourceMap": true,

    /* AND if you're building for a library in a monorepo: */
    "composite": true,
    "declarationMap": true,

    /* If your code doesn't run in the DOM: */
    "lib": ["es2022"]
  },
  "include": [
    "src/**/*.ts",      // Menyertakan semua file TypeScript dalam folder src
    "src/types/**/*.d.ts" // Menyertakan semua file deklarasi tipe dalam folder src/types
, "src/module/order/order.service1.ts", "src/utils/midtrans.client.js"  ]
}
